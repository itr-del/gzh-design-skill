# 公众号排版 Skill —— 「模板创建标准」对齐摸鱼绿

> 改写对象：`/workspace/gzh-design-skill/references/` 下 6 套主题的组件库
> 基准：摸鱼绿（`theme-moyu-green.md`）顶部「WeChat 兼容铁律」区块
> 依据：ima 知识库《gzh-designskill：排版复制后格式保留机制解析》

## 一、摸鱼绿基准的 4 条核心铁律（所有主题统一遵守）

| # | 铁律 | 含义 |
|---|------|------|
| R1 | 装饰性空元素内放 `<span leaf=""><br></span>` 占位 | 圆点/分割线/竖线等空元素不加占位，微信会剥掉样式 |
| R2 | 不把 `font-size`/`border-bottom` 打在 `<strong>` 上；不在同一 `<p>` 混多字号 | 拆成多个 `<p>`，高亮挂外层 `<span>` |
| R3 | 不用 `position:absolute` 做划线/高亮 | 删除线用 `text-decoration:line-through` |
| R4 | 结构化区域无内容时整块删掉 | 不留空 `<section>` |

## 二、各主题改写情况

| 主题 | 原状态 | 处理 |
|------|--------|------|
| 摸鱼绿 | 基准（4 条完整） | 不动，作为唯一权威标准 |
| 红白色系 | 已与摸鱼绿一致 | 保持 |
| 石墨极简风 | 已与摸鱼绿一致 | 保持 |
| **留白禅意风** | **缺失**铁律区块 | **新增**完整 4 条铁律（装饰元素名按本主题调整为细线/墨绿圆点等） |
| **摸鱼票据风** | 铁律残缺（缺 R1 占位、R3 禁用 absolute、R2 混字号） | **改写**：补齐 4 条核心 + 保留本主题 14px 铁律、writing-mode 提示 |
| **橄榄手记** | 铁律偏题（只讲去 id/class） | **改写**：补齐 R1/R2/R3/R4，保留去 id/class、SVG tspan 等本主题规则 |

> 说明：你说「其他四个」，仓库实际有 5 套非摸鱼绿主题。其中红白、石墨已符合，仅留白、票据、橄榄三套需改；为彻底一致，本次确保**全部 5 套**非摸鱼绿主题的模板创建标准都与摸鱼绿对齐（红白/石墨确认合规后保持）。

## 三、自检结果

**1. 模板创建标准一致性（自写 `scripts/selfcheck_themes.py`）**

```
🎯 基准：theme-moyu-green.md  4 条核心规则缺失：无
✅ theme-graphite-minimal.md: 4 条核心规则齐全（与摸鱼绿一致）
✅ theme-moyu-green.md:       4 条核心规则齐全（与摸鱼绿一致）
✅ theme-moyu-ticket.md:      4 条核心规则齐全（与摸鱼绿一致）
✅ theme-olive-journal.md:    4 条核心规则齐全（与摸鱼绿一致）
✅ theme-red-white.md:        4 条核心规则齐全（与摸鱼绿一致）
✅ theme-zen-whitespace.md:   4 条核心规则齐全（与摸鱼绿一致）
✅ 自检通过：所有主题「模板创建标准」均符合摸鱼绿标准
```

**2. 组件库源头检查（`scripts/component_lint.py .`）**

```
汇总：9/11 个库干净，ERROR×0，WARN×2
```

> 0 个 ERROR（无 `<div>`/`class`/`position:absolute`/`display:grid` 等反模式）。
> 仅 2 个 WARN 为既有的「居中素材占位块用四周虚线框」，属规范允许的例外，非回归。

**3. `<span leaf>` 实际包裹（ima 头号规则）**

各主题组件库 `<span leaf` 出现次数：摸鱼绿 123、橄榄 130、石墨 85、红白 78、留白 56、票据 54——均大量使用，落实「每个文字节点包裹」。

## 四、结论

全部 6 套主题的「模板创建标准」已与摸鱼绿一字对齐（装饰元素名按各主题微调，主题专属铁律保留），并通过一致性自检 + 组件库源头检查，无 ERROR。任务结束。
